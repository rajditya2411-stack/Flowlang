# Nested If Statements
lit x = 10
lit y = 20

if x > 0 {
    say("x is positive")
    if y > 0 {
        say("y is also positive")
    } else {
        say("y is not positive")
    }
} else {
    say("x is not positive")
}
